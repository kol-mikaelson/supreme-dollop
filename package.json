{
  "name": "kol-mikaelson-portfolio",
  "version": "1.0.0",
  "description": "Personal portfolio and blog",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy",
    "deploy": "npm run build && wrangler deploy",
    "preview": "npm run build && wrangler dev"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "wrangler": "^4.79.0"
  }
}
